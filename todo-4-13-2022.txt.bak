Pattern pattern = Pattern.compile("^[^.\\\\\\\\/:*?\\\"<>|]?[^\\\\\\\\/:*?\\\"<>|]*");

977 issue -> 978 issue 

hashtag'ler var 

svr_admin
sistas_1

2 yeni alan yok

svr excel test et

callPartyContext searchEngine

20000 page sınırı

1069 issue -> Sistas Voice Solution

action.visible abstract class olduğu için değiştirilemiyor

cancel kapanıcak create var 

create save yapıldıktan sonra gelicek

config key config value

device agent yeni eklenilen alanlar içinde 

1057 issue -> svr_instance_name type 

provided by -> enum voice service_provider olarak enum var

-----------------------------------------------------------------

1) Device Ekranı
ProvidedBy:
- ServiceProvider enum alanı oluşturulacak. (genesys, alcatel, recorder olacak).
- Provided By alanı multiselect(bootstrap) olacak ve ServiceProvider seçilecek.
- Seçilen ServiceProvider lar (providedBy => #genesys##alcatel##recorder#) formatında oluşturulacak.
- Eski haline destek verebilmek için multiselect olan alana (#genesys##alcatel##recorder#) formatında yapıştırıldığında multiselectte yapıştırılan enumlar seçili olarak gösterilecek.

Note: (VoiceAgentList ve VoiceServerList için ortak olarak yapılacak adımlar:)
- Api ile config instance lar alınacak. (ConfigServerResource -> /api/config-server/instances) - ConfigType.MICROSERVICE
- Dönen isteği intanceType -> VOICE_AGENT olanları ve intanceType -> VOICE_SERVER olanları filtreleyip birer parametreye atılacak.

VoiceAgentList:
- VoiceAgentList alanı multiselect olacak ve api dan alınan voice agent instanceName leri gösterilecek.
- Seçilen instanceName ler (voiceAgentList => Agent001, Agent002, Agent003) formatında oluşturulacak.

VoiceServerList:
- VoiceServerList alanı multiselect olacak ve api dan alınan voice server instanceName leri gösterilecek.
- Seçilen instanceName ler (voiceServerList => Server001, Server002, Server003) formatında oluşturulacak.

2) Agent Ekranı
ProvidedBy:
- ServiceProvider enum alanı oluşturulacak. (genesys, alcatel, recorder olacak).
- Provided By alanı multiselect(bootstrap) olacak ve ServiceProvider seçilecek.
- Seçilen ServiceProvider lar (providedBy => #genesys##alcatel##recorder#) formatında oluşturulacak.
- Eski haline destek verebilmek için multiselect olan alana (#genesys##alcatel##recorder#) formatında yapıştırıldığında multiselectte yapıştırılan enumlar seçili olarak gösterilecek.

3) ProvidedBy alanı search(Device ve Agent Ekranı için)
- #740 EVA-Sonlandıran Katılımcı issue sunda ReleasingParty alanı için yapılan değişiklik ile aynı şekilde yapılabilir.

inputlar kolon tipine göre geliyor kolon tipi enum olarak belirlenmiş

pageable 

https://v6.material.angular.io/components/chips/overview

chips kullanılabilir

dinamik input'tan fetch api yapmaya çalıştığımızda sonsuz request atıyor

https://stackblitz.com/edit/angular-w1alg4-vt1o4z?file=app%2Fapp.component.ts



ng sağ taraf aktif olucak sol taraftan tıklanınca 

sağ tarafa hashtag li olarak aktarılacak

sağ tarafta ekli gelen yerlerin sol tarafta kontrolü de lazım olabilir

arraydeki label'ların hashtag'li biçimde maplememiz gerekiyor

yapıştırılacak string'in formata uygun olması gerekiyor

ikiside aynı api'den alıcak -> 

configInstanceName

optionsInstanceTypes : name : VOICE_AGENT

VOICE_AGENT

VOICE_SERVER

ngAfterViewInit

pageInstance


onClick function'una subscribe ol

kullanıcı input'tan sildiğinde veya yapıştırdığında

#selam##evet##tyyuu#

listede olmayan değer valid olmayacak

custom validator 

server list içindeki değerleri input içinde sürekli kontrol eden bir fonksiyon yazılmalı

validators regex + !list.includes(str)

alan required değil control nasıl olacak

eva issue 1052 1067

1067 -> call_type
http://localhost:9001/page-evaluation-monitoring

1052 -> kolonlar değişecek ve tıklanınca açılan yerde sağa sola kaydırma özelliği gelicek

startTime,  name,  surname,  fromExtension , toExtension 
, callType , durationInSeconds  ,ArananSkill  ,AktarılanSkill  ,SonlandıranKatılımcı

[ "startTime", "ani", "dnis", "extension", "agentId", "durationInSeconds", "callType", "connectionId", "operator.userId", "callContext.conversationContext.conversationType", "callContext.globalCallId", "gridActions" ]

startTime,  name,  surname,  ani , dnis 
, callType , durationInSeconds  ,ArananSkill  ,AktarılanSkill  ,SonlandıranKatılımcı


ikinci tıklanmada boyutu azalıcak
2/3

1/3

flex-direction: row; box-sizing: border-box; display: flex;

flex-direction: row; box-sizing: border-box; display: flex;

sadece callPartyContext sayfasında değişim yapılacak,


validation hatası devam edilecek

select butonu küçülecek 

yapıştırınca tek tek çıkacak yuvarlak şekilli olabilir

tik işareti hashtag olacak

yapılan geliştirme svr'nin dinamik altyapısına uygun olmalı

this.field.metadata.columnType(NUMBER,DATE,DOUBLE,BOOLEAN) kısımına göre switch yapılmış 

buraya nasıl olmalı

switchType for input 

ismine göre alamayız

her bir colon için ayrı ayrı set'lemek gerekecek
her sayfa için form component isimlerine(Metadata nameleri) göre alınıp enum bir değere atılabilir 

status->(INPUT)
deviceIp->(INPUT)
voiceAgentList->(MULTISELECT)
voiceServerList->(MULTISELECT)

setType(BOOLEAN),
setType(MULTISELECT),
setType(NUMBER)

config version 3.5 version değiştirilince post edemez

commit messagelarına  version eklenicek

https://surveys.jetbrains.com/s3/developer-ecosystem-survey-2022-sh?pcode=150604351042086216

controlFromInput nasıl alınacak dinamik şekilde

optionsInstanceTypesVoiceAgent  dinamik function dönmeli ngFor içinde

ngFor için pipe olabilir mi

search engine compact halinde tüm kolonlar görülecek

search engine yeni kolon

büyük değişiklik yok

küçük büyük sırada olabilir yada kerem bey'in verdiği sıralamada olucak

click sırasında gelmeli ve .instance Name için generic bir çözüğm yapılmalı

pipe ile filter edilebilir mi

return koymamız lazım

kodu okunabilir hale getirmek için değişiklik yaptık
controlFromInput fonksiyonu çalışmıyor

registry 

config altına json koyalım bunu kalkarken alıyor mu 

version kısmına bakılacak

cmd.configList olmak zorunda

json olarak yaml olarak kaydetmeliyiz

spring uygulamaları basic auth destekliyor 

header jwt token expire oluyor client bazlı ayarlayamıyoruz

authorization header

basic ve oauth2(jwt) desteklememiz gerekiyor

cloud config client jwt .yml olarak kaydetmeceğiz

json olarak 


config ve template 

içeriğini 

json to yml


        C:/Users/meteatbas/Desktop/Döküman/YamlDump

style="overflow-y: auto;max-height: 280px" 
		
any'lere type eklenicek

dropdown scrool bakılacak +

id kaldırılacak +

optional olanlar kaldırılacak +

required length'ine göre bakmalı ?

function isimleri değişecek

satırlara 3. tıklayışta regexValidator fonksiyonuna giriyor

version test 

template dökümantasyon

post config

http://localhost:8888/api/template?instanceType=VOICE_SERVER&version=v3.9.8&configType=MICROSERVICE&description=templatecomment

convert'te problem var

yaml to json converter

spring.profiles kaldırırsan json olarak alabiliyor
objectMapper neden kullanıyoruz İrem'e sor

get yaparken belli bir formata göre çekiyoruz

installation document

windows prerequest 

java git 

registry'siz yaml'dan kaldırma

enable disable yapılacak registry için

Upload Config
http://localhost:8888/api/config/?instanceType=VOICE_SERVER&instanceName=AGENT0010&version=v3.9.8&configType=MICROSERVICE&profile=SVR&description=selam
{
        "bar": [
            {
                "test1": 0
            },
            {
                "test2": "string"
            }
        ]
}

{
    "bar": {
        "configValueType": "CUSTOM",
        "configValueCount": "LIST",
        "displayName": "is CallListening Enabled",
        "defaultValueAsJson": null,
        "description": "Enables CallListening, Capturer will send CallStarted/CallEnded message to CallListen Host:port",
        "sectionName": "CallListening",
        "encrypted": false,
        "rule": "${bar}.?[#this.get('test1') eq 0].size() == 1 and ${bar}.?[#this.get('test2') eq 'test'].size() == 1"
    }
}



OAuth2ClientId:
  configValueCount: "SINGLE"
  configValueType: "STRING"
  displayName: "OAuth2ClientId"
  description: "OAuth2ClientId"
  sectionName: "Sistas Configuration Server"
  encrypted: false
  rule: null
  restartRequired: false
  defaultValue: "configuration-server"
clusteredInstanceCount:
  configValueCount: "SINGLE"
  configValueType: "LONG"
  displayName: "clusteredInstanceCount"
  description: "Deprecated"
  sectionName: "General"
  encrypted: false
  rule: null
  restartRequired: true
  defaultValue: 1
commonConfigAgent:
  configValueCount: "SINGLE"
  configValueType: "STRING"
  displayName: "commonConfigAgent"
  description: "Json config list. config list will override All Agents config. ex:\
    \ {\"MaxCacheExecutionActorCount\":\"2\",\"IsRestartProcessManagement\":\"true\"\
    }"
  sectionName: "CommonConfig"
  encrypted: false
  rule: null
  restartRequired: false
  defaultValue: ""
commonConfigRecorder:
  configValueCount: "SINGLE"
  configValueType: "STRING"
  displayName: "commonConfigRecorder"
  description: "Json config list. config list will override All Recorder config. ex:\
    \ {\"MaxCacheExecutionActorCount\":\"2\",\"IsRestartProcessManagement\":\"true\"\
    }"
  sectionName: "CommonConfig"
  encrypted: false
  rule: null
  restartRequired: false
  defaultValue: ""
commonConfigServer:
  configValueCount: "SINGLE"
  configValueType: "STRING"
  displayName: "commonConfigServer"
  description: "Json config list. config list will override All Server config. ex:\
    \ {\"MaxCacheExecutionActorCount\":\"2\",\"IsRestartProcessManagement\":\"true\"\
    }"
  sectionName: "CommonConfig"
  encrypted: false
  rule: null
  restartRequired: false
  defaultValue: ""
  
rule array dökümanı yaptım
  
maven build etme chatbot docker'ize edilmesi

(git local path problemi) docker'da yetkisi yok

custom validation review notları

config yml review notları

document testi yapılacak

app'in altında deneyelim

v0.0.2 release notları çıkabiliriz

versiyon artırımı için jar oluşturulacak

0.0.1 version

todo: commit versions and rollback with jgit api

This is the first "production ready" release.  :smile:
  

## :star: New Features
### :exclamation: Embedded Spring Cloud Config Server
- #93 Custom config value type is added
---

## 🐞  Bug Fixes
### :exclamation: Embedded Spring Cloud Config Server
- #93 Custom config value type is added
- #97 Config an Template files format has been changed
- #102 Commit version added to Commit Message
---

### :exclamation: APIs
- Config create, update and delete APIs are implemented.
- Template create, update and delete APIs are implemented.
- Instances and instance types get APIs are implemented.
---

### :exclamation: Validation
- Config items uploaded and editted are validated by config template.
---

### :exclamation: Documentation
- #103 Installation updated with `Prerequisite`
- #93 Template document updated with examples.
---

### :exclamation: Windows Installation
- Windows installation setups and steps are developed.